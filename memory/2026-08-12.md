
## Draft status reconciliation (17:15) — IMPORTANT FIX
- Alec noticed Gmail had almost no drafts vs 32 leads marked "drafted" in arlo_findings.json.
- Root cause: Iris marks leads "drafted" but NEVER updates to "sent" when Alec sends them. Status drifted badly.
- Built `agents/reconcile_draft_status.py`: checks Gmail sent-mail (incl trash) per drafted lead, marks confirmed-sent ones "sent". Ran it.
- Result: 15 = actually SENT (in Sent mail), 16 = DELETED DUPLICATES (Alec trashed them — duplicate company entries, e.g. two Palm Beach CPA, two Fishman, two crrtoday.com), 1 = genuinely pending draft (Highlight Realty).
- Marked 16 as status `deleted_duplicate` (+note). Only 1 true draft remains: **Highlight Realty Network** (highlightrealty@aol.com) — unsent, needs Alec action.
- Reply scanner OUTREACHED set now = {drafted, sent, replied}; skips deleted_duplicate. Verified.
- LESSON: Iris/Arlo status never auto-advances drafted→sent. Reconcile script should run after each batch + after sends. Consider wiring into cron.
