# Memory Log - 2026-07-31

## Model Routing Repair
- Root cause of failed assistant/webchat turns: runtime config still routed through deprecated `ollama/kimi-k2.5:cloud`, stale OpenAI Codex model entries, and Anthropic fallback.
- Anthropic API key is present, but Anthropic returns insufficient credits/billing disabled. Excluded Anthropic from automatic routing until billing is fixed.
- OpenRouter key works. Smoke-tested `openrouter/openai/gpt-5.6-luna` and `openrouter/moonshotai/kimi-k3` successfully.
- Updated runtime defaults in `~/.openclaw/openclaw.json`: primary `ollama/qwen3-coder:latest`, fallback `ollama/llama3.1:latest`, then OpenRouter Luna/Kimi.
- Updated `~/.openclaw/config.json`, workspace `routing-config.json`, `MODEL_ROUTING.md`, `MODEL_ROUTING_V2.json`, `scripts/model_routing_healthcheck.sh`, and `scripts/sync_model_routing.sh`.
- Backups created with suffix `.bak.model-fix.20260731_163545`.
- Validation passed: `openclaw config validate`, `openclaw models status`, and `bash scripts/model_routing_healthcheck.sh`.

## Site Repair Sprint (completed 2026-07-31 ~21:10 ET)

Full audit + repair of theonegroup.info, deployed live (deploy 6a6d4686f6e9cca95943bf87).

**Done:**
- BUILT /lead-response.html — full service page (was a 404 linked from 5 key pages: index, pricing, contact, competitor-intelligence, compliance-content). $297/mo + setup, links to qualify + missed-call-calculator.
- Fixed events.html /mmw-2026.html -> /archive/mmw-2026.html
- Consolidated duplicate blog posts: 301 redirects in netlify.toml (ai-myth-data -> ai-data-myth, building-in-public -> building-in-public-week3). Originals moved to archive/. blog.html duplicate card removed, links repointed.
- Unique meta descriptions: truerep, free-ai-audit, missed-call-calculator (all 3 shared the same generic one).
- Cross-linked audit ladder: free-ai-audit.html upsells $2,500 AI Visibility Audit; ai-audit.html points down to free audit.
- Homepage hero now promotes missed-call-calculator as lead magnet.
- Deploy killed once by 120s timeout mid-upload; re-ran with longer timeout — fine.

**Still open (from audit recommendations, not yet done):**
- Add JSON-LD/FAQ schema for AI search citations
- llms.txt file
- Video content (competitors all use YouTube demos)
- H1/title audit on remaining pages
- Consider Skool/community + personalized demo videos in outreach

## Round 2: AI-search optimization + growth deliverables (completed ~21:20 ET)

**Deployed (deploy 6a6d476f40083d7a751c716c), all verified live:**
- llms.txt at theonegroup.info/llms.txt (services, free tools, contact)
- JSON-LD schema on 22 pages: ProfessionalService+WebSite (homepage), FAQPage (pricing), Service (6 service pages), Article (14 blog posts). All JSON validated before deploy.
- NOTE: schema JSON uses json.dumps default spacing — grep pattern is `"@type": "X"` with space.

**Drive deliverables:**
- 2026-08-video-content-plan.md — 4 prioritized videos, hooks, CTAs, production notes
- 2026-07-31-github-radar-deep-dive.md — open-connector (spike now), loop-engineering (content play), ego-lite (internal analytics fix first, security review before client use)

**Next actions queued:**
- Spike oomol-lab/open-connector (1-2h): Google account -> Calendar read + Sheet write via MCP
- Dante: "loop engineering" blog post assignment
- ego-lite: possible fix for broken Twitter engagement tracking
- Video #1 ("8-agent system demo") week of Aug 4

## Open Connector Spike: PASS ✅ (~21:35 ET)

- Repo at spikes/open-connector, restart: `npm run dev` (API :3000, console :5173, MCP :3000/mcp)
- Verified: 1,210 services, no-auth exec, GitHub PAT store + authenticated exec (github.get_current_user -> @akennedytog), MCP tools (list_apps/search_actions/get_action_guide/execute_action)
- Action contracts lazy-load by ID (e.g. googlecalendar.list_events) even though ?service= list can be empty — don't rely on list endpoint for discovery; use MCP search_actions
- Google OAuth NOT configured — needs Alec to create Google Cloud OAuth app (15 min manual, steps in Drive report) or use OOMOL hosted runtime
- Verdict: ADOPT for client implementations; enables "connects to 1,000+ tools" claim on ai-optimization.html
- GitHub connection (default, akennedytog PAT) persists in spike's SQLite

## Open Connector wired into OpenClaw ✅ (~21:45 ET)

- Registered: `openclaw mcp add open-connector --url http://localhost:3000/mcp --transport streamable-http`
  - GOTCHA: `--transport http` is rejected by bundle-mcp; must use `streamable-http`
- Probe: 5 tools (list_apps, list_connections, search_actions, get_action_guide, execute_action)
- Config: mcp.servers.open-connector in ~/.openclaw/openclaw.json
- Persistence: launchd agent `com.theonegroup.open-connector` (KeepAlive + RunAtLoad), logs at spikes/open-connector/logs/launchd.{log,err}
  - manage: launchctl bootstrap/bootout gui/$(id -u) ~/Library/LaunchAgents/com.theonegroup.open-connector.plist
- Verified after launchd restart: probe 5 tools, github.get_current_user -> akennedytog, GitHub connection persisted in SQLite
- TODO: Google OAuth app (manual, Alec) to unlock gmail/calendar/sheets actions

## Google OAuth COMPLETE ✅ (~21:55 ET) — Calendar + Sheets + Gmail all live

- OAuth app "open-connector" in existing GCP project `clawd-agent-integrations` (number 601004929144)
- Consent screen: new Google Auth Platform UI (Branding/App name, Audience/test users, Clients)
- All 3 services connected as oauth2 default connections in connector SQLite
- VERIFIED LIVE:
  - googlecalendar.list_calendars -> real calendar list
  - googlesheets: created "OpenClaw Connector Test" (id 1aBkq6NdkDGKMW-OtgPYqE_NNQ6WP__RDz6QTub8l6Aw), values_update + spreadsheets_values_append + batch_get round-trip PASS
  - gmail.get_profile -> akennedy@theonegroup.info (358 messages/258 threads)
- GOTCHAS learned:
  - Some action ids have digit suffixes (create_google_sheet1) — don't grep [a-z_] only
  - values_update/append REQUIRE valueInputOption (USER_ENTERED/RAW)
  - create_spreadsheet_row inserts blank rows; use values_update/append for data
  - Use MCP search_actions for discovery; ?service= list endpoint is unreliable
- NOTE: consent screen in "testing" mode -> tokens expire after 7 days. Republish app or re-auth weekly until published.

## OAuth app PUBLISHED + re-authed ✅ (~21:58 ET)

- App published to production in Auth Platform (Audience -> Publish app) — tokens no longer expire every 7 days
- Re-ran all 3 consents post-publish (old testing-mode tokens would have expired); new tokens are permanent
- Re-verified all 3: calendar list, sheets batch_get (test sheet intact), gmail profile — all live
- Unverified-app warning will still show on future consent screens, but no re-auth needed going forward (unless scopes revoked or 6mo unused)
- Google OAuth stack COMPLETE: calendar, sheets, gmail — agent-accessible via open-connector MCP

## Heartbeat email triage + Arlo Sheets + site claim ✅ (~22:10 ET)

1. HEARTBEAT.md: added step 3 email triage via connector (gmail.fetch_emails, query "is:unread newer_than:1d"). Ping only for human/sales emails. Connector restart cmd: launchctl kickstart gui/$(id -u)/com.theonegroup.open-connector. Verified live (0 unread today; general unread query returns real subjects).

2. Arlo -> Google Sheet pipeline DEPLOYED:
   - Sheet "TOG Leads — Arlo" (id in data/lead_sheet_id.txt)
   - scripts/arlo_sheets_sync.py (append-only, state in data/lead_sheet_state.json, first run skips backfill)
   - run_arlo_daily.sh updated to sync after each run
   - Tested: 20 rows synced, verified in sheet
   - ⚠️ IMPORTANT FINDING: Arlo generates SYNTHETIC leads (random names/phones/emails from patterns). The 1,334 "leads" in data/arlo_findings.json are placeholders. Needs real-research rewrite (Tavily/Firecrawl) — flagged to Alec, biggest open gap.

3. ai-optimization.html: added "1,000+ Tool Integrations" feature block. Deployed (6a6d510bfa8d886a40b09b54), verified live.

## Arlo REBUILT for real research ✅ (~22:15 ET)

- NEW: agents/arlo_real.py — Tavily search (6 credits/day, ~180/mo of 1,000 free) + ollama/llama3.1 extraction (free) -> real business names/phones/addresses
- Rotation: deterministic industry x city grid, state in data/arlo_rotation_state.json (12 industries x 10 cities)
- Dedupes by lowercase name vs existing findings; scores 5-10 (phone +2, website +2, address +1); priority from score
- Old synthetic agents/arlo.py PRESERVED but run_arlo_daily.sh now calls arlo_real.py
- First real run: 20 HVAC leads (WPB/Boca/FLL/Delray), 18/20 with real phone numbers, 8/20 addresses
- PURGED 1,314 synthetic leads -> data/arlo_findings_synthetic_archive.json (archived, not deleted)
- Sheet "TOG Leads — Arlo" cleared of test rows, now contains ONLY real leads (20)
- Cron unchanged (9 AM daily), sheet sync runs after
- Watch: llama3.1 extraction took ~4 min for 4 queries — if cron machine is busy it may take longer; fine for 9 AM batch
- Iris/Dante downstream now get REAL data

## Iris REAL + lead enrichment ✅ (~22:20 ET)

- scripts/enrich_leads.py v2: DDG website discovery + aggressive email extraction (cfemail decode, entity decode, [at]/[dot], 8 paths, DDG email fallback)
- REALITY: HVAC/local-service sites are phone-first — only 3/20 publish emails (southfloridaair, abrahamac, fortlauderdaleacservices). This is industry ground truth, not scraping failure. Website validation clears bad DDG matches (name tokens must appear in domain).
- agents/iris_real.py: email leads -> personalized Gmail DRAFTS via ollama (max 5/day), phone-only leads -> daily "call list" draft to Alec with 30-sec opener script. NOTHING SENDS WITHOUT ALEC.
- First run: 3 email drafts + 1 call-list draft (15 leads) created in Gmail, verified live
- Sheet: added Email column, refreshed all 20 rows w/ websites+statuses
- Status lifecycle: new -> enriched -> drafted | call_listed
- Daily runner now: arlo_real -> enrich -> sheet sync (runner updated earlier; enrich step needs adding — TODO next session)
- Open item: SMS/call capability would double coverage (phone-first industry). Twilio via connector catalog when ready.

## Iris outreach P.S. blurb (22:30 ET)
- Alec: outreach emails should include a quick overall services blurb (P.S., one sentence) in case the missed-call pitch doesn't fit the lead: AI visibility audits, competitor monitoring, AI coaching. Added to iris_real.py prompt. Applies from next batch onward (current 3 drafts unchanged).

## Default model fix — ROOT CAUSE + resolution (23:15 ET) ✅

**Root cause:** ollama/qwen3-coder:latest (30B) as gateway default was unworkable — every session carries ~20k tokens of system context; stress test showed >6 min for a 20k prompt on the MacBook (36GB RAM). Gateway times out (~2 min) -> "Ollama API stream ended without a final response" -> 3 retries -> "agent run failed before producing a reply." The garbled WhatsApp replies at ~23:00 were qwen3-coder choking on context.

**Fix:** default primary changed to openrouter/openai/gpt-5.6-luna ($1/$6 per 1M — 3x cheaper than kimi-k3 $3/$15) in openclaw.json (agents.defaults.model.primary + agents.list[main].model). Backup: openclaw.json.bak.default-fix.*. Gateway restarted, verified live.

**Ollama still used (correctly):** direct API calls from scripts (arlo extraction, iris drafting — small prompts, batch, free). Just not as the interactive gateway default.

**Open item:** cron jobs still referencing kimi-k2.5:cloud (deprecated) — migrate to gpt-5.6-luna next session.

**For Alec:** run /model default in the pinned main webchat session to drop the kimi-k3 override.
