## theonegroup-v2 Legal & Compliance Hardening (2026-08-08)

Alec asked to bring theonegroup.info v2 up to date on legality/security. Confirmed HIPAA does NOT apply (no PHI/medical data collected — marketing site only). Did NOT do legal certification — only code-level hardening + policy docs.

### Changes deployed (all LIVE on theonegroup.info)
1. **Cookie consent banner** — GA4 + Plausible are now CONSENT-GATED. Static script tags removed from head; they load dynamically ONLY after visitor clicks Accept (localStorage key `tog_cookie_consent`). trackEvent() no-ops before consent. Default state = no tracking.
2. **Terms of Service page** (/terms) — created from scratch: services, IP, no-advice disclaimer, payments, liability limits, indemnification, FL governing law.
3. **Expanded Privacy Policy** (/privacy) — was 21 lines, now full: data collected, cookie/analytics disclosure, third-party processors (Google Analytics, Plausible, HubSpot, Resend, Calendly), retention, security, user rights (GDPR/CCPA), children's privacy, changes.
4. **ADA accessibility** — contact form: aria-required, aria-label on all 5 fields, autocomplete (name/email/organization/tel); newsletter input: aria-label + autocomplete=email. Skip-to-content link already existed. No <img> tags exist (all CSS/SVG), so alt-text wasn't a blocker.

### Verified live
- Consent banner present, no static GA/Plausible tags, terms + privacy serve HTTP 200.
- Deploy: 6a7... ready 2026-08-08 15:46 UTC, 9s build.

### Not done (needs human/professional)
- No formal legal certification (needs qualified counsel).
- WCAG contrast/keyboard-nav not fully verified — would need real a11y audit (Lighthouse/axe).

## ADA / Accessibility Audit - COMPLETE (2026-08-08)

Ran Lighthouse (v13.4.1) accessibility audits on all 6 pages of theonegroup.info.

**Result: ALL 6 PAGES = 100/100 accessibility score.** ✅

Issues found & fixed (all deployed live):
1. Footer headings used `h4` with no preceding h3 → changed to `h2` (heading order)
2. Footer copyright `text-gray-500` on `bg-gray-50` → `text-gray-700` (contrast)
3. "Links rely on color" (WCAG 1.4.1) → added persistent `underline` to all inline text links (consent banner, mailto, tel, blog subscribe, index links)
4. Blog AI headlines number `text-brand-200` on white → `text-brand-500` (contrast)
5. Case-studies italic quotes `text-gray-400` → `text-gray-600` (contrast)
6. Home testimonials: attribution `p.text-xs` and blockquotes lacked `dark:` variants → `text-gray-600` + `dark:text-gray-300` (dark mode contrast)
7. Ticker meta/number `text-gray-500/600` + dark-mode `dark:text-gray-400` → darkened (contrast)
8. Services small text `text-gray-400` → `text-gray-600` (contrast)

Form accessibility already done in prior pass (aria-required, aria-label, autocomplete).
Site has zero `<img>` tags (all CSS/SVG) so alt-text N/A. Skip-to-content link present.

**Caveat:** Lighthouse = automated WCAG check, not a substitute for manual testing by a screen-reader user or certified auditor. But this catches the vast majority of common failures. All pages clean.

## SEO / Performance / Best-Practices - COMPLETE (2026-08-08)

Ran Lighthouse SEO + best-practices + performance on theonegroup.info.

**Results (all live):**
- SEO: 91 → **100**
- Best-Practices: 96 → **100**
- Performance: 88 → **100**

### Fixes deployed
1. **robots.txt was MISSING** (returned 404) → created in public/ with Allow all + Sitemap reference. Now HTTP 200.
2. **No custom 404 page** → created branded 404.astro (Netlify was serving generic page).
3. **favicon.ico was MISSING** (browser 404 error) → generated .ico from PNG via sips, added favicon links + apple-touch-icon to layout head. Also copied favicon.svg/png from old site.
4. **og-default.png referenced but missing** (OG image 404 risk) → created at public/assets/images/og-default.png.
5. **"Learn more" links non-descriptive** (SEO) → changed to "Learn more about AI Audit/Implementation/Competitor Intel" + added aria-labels.
6. **Google Fonts render-blocking** (perf) → async font load via preload+onload media trick + noscript fallback; added preconnect for plausible.io + googletagmanager.

### Verified live
- robots.txt 200, favicon.ico 200, custom 404 serves, sitemap-index.xml valid (8 pages), descriptive link text present.
- Home + Services both SEO 100 / Best-Practices 100.
- Performance 100 (FCP/LCP ~1.0s, CLS 0.03, TBT 0ms).

**Caveat:** Performance/SEO scores are desktop-mobile dependent; these are solid automated results, not a guarantee of rankings. robots.txt + sitemap submission to Google Search Console still worth doing if not done.

## Lexus TX Tracker Sheet (created 2026-08-08)
- Sheet: "Lexus TX Tracker - Car Offers"
- ID: 1UL4IbDDVT5ClWb0Pqa1j_C0xgWPHtIYfUDP7q8HtBJA
- URL: https://docs.google.com/spreadsheets/d/1UL4IbDDVT5ClWb0Pqa1j_C0xgWPHtIYfUDP7q8HtBJA/edit
- Columns: Date Added | Year | Model | Trim | Price | Mileage | Location | Distance | Certified? | VIN | Stock # | URL | Exterior | Interior | Dealer | Phone | My Offer | Offer Date | Status | Notes
- Current focus: 2025 TX 500h F SPORT Premium L/Certified, Margate FL, $69,999, 10,086 mi (row 2) - recommended offer $68k
- Alec lives in Ft. Lauderdale, FL (not Chicago)

## pitrowmiami.com - Full Audit & Fixes - COMPLETE (2026-08-08)

Ran full Lighthouse audit (accessibility, SEO, best-practices, performance) + legal/compliance pass on pitrowmiami.com, mirroring theonegroup.info work.

**Scores:**
| Category | Before | After |
|----------|--------|-------|
| Accessibility | 75 | **100** (index) |
| SEO | 100 | **100** |
| Best-Practices | 100 | **100** |
| Performance | 67 | **81** |
| Terms page a11y | — | **100** |
| Privacy page a11y | — | **100** |

### Accessibility fixes
1. **Form labels**: `<label>` had no `for=`, inputs no `id=` → added associations. **BUG encountered**: my regex stripped label TEXT (created empty `<label for=...></label>`) — restored "Name *" etc. Lighthouse needs visible text in labels.
2. **Newsletter input**: added aria-label + unique id (was accidentally given duplicate bk-email id — fixed).
3. **Contrast**: site is dark (zinc-800/900 on black). Global bump text-gray-400→300, gray-500→400, gray-600→500. But regex only matched `class="text-gray-400 ` (gray first in class list) — missed `class="text-lg font-semibold text-gray-400 mb-2"`. Fixed with position-agnostic replace.
4. **Footer**: text-zinc-500 links → zinc-300 + underline; copyright gray-500→400.
5. **Red links**: brand red #e10600 fails on dark bg → brightened to #ff3b30 for TEXT links (kept #e10600 for buttons w/ white text, which pass).
6. **Terms/privacy pages**: added `<main>` landmark (was missing), zinc-300→200, brightened red back-link + mailto links.

### Consent/Legal
- **GA (G-B5NV9GJ810) fired unconditionally** → consent-gated: loads only after Accept; localStorage `prm-consent`. Fixed on index, privacy, terms (all 3 had GA).
- **Cookie consent banner** added (bottom-fixed, Accept/Decline, links privacy.html).
- **Terms of Service page** created (terms.html), linked in footer + sitemap + privacy page.

### Performance (81, remaining = structural)
- Removed dead `google-site-verification` placeholder meta.
- Added preconnect for googletagmanager + tailwind CDN.
- Added width/height to all images (hero 600x800, logo 1024, qr 400).
- **Remaining**: Tailwind Play CDN (cdn.tailwindcss.com) is render-blocking + ~127KB. This is the architectural limit of the static site using CDN Tailwind instead of compiled CSS. Fix = compile Tailwind into a static CSS file (bigger refactor).

### SEO (already 100, verified)
- robots.txt ✅, sitemap.xml ✅ (added terms.html), OG images ✅, canonical ✅, JSON-LD LocalBusiness ✅.

### Deploy gotcha (IMPORTANT)
**Deploying from /tmp/pitrowmiami-deploy requires the .netlify link state.** First deploy from a fresh /tmp copy went to a THROWAWAY site `creative-brigadeiros-7df157.netlify.app` (accidentally created). Fix: `cp -r .netlify /tmp/pitrowmiami-deploy/` BEFORE `netlify deploy --prod --dir .`. Then it deploys to pitrowmiami.com correctly. Deleted the stray site after.

**Netlify pretty-URL note:** live site rewrites `href="terms.html"` → `href='/terms'` at the edge (Netlify auto pretty-URL). Both resolve fine; source keeps `.html` links.

## Heartbeat 12:47 ET (2026-08-08)
- Model routing healthcheck: ALL PASS ✅
- open-connector (localhost:3000) DOWN — kickstart attempted, did not recover. Email triage skipped this cycle. Flag for next heartbeat / possible manual restart.
- No urgent items. Site work (theonegroup + pitrowmiami) complete.

## TX 500h Lease Decision (12:50 PM) - LEASE WINS
- Alec confirmed all 3 lease-favorable factors: drives <10k mi/yr, wife's LLC business, new car every 2-3yr
- Compared $802/mo Pembroke Pines deal vs DSR broker benchmark ($959/mo $2,649 due, same car)
- VERDICT: $802/mo is competitive - $36/mo better effective ($996 vs $1,033); $1,301 cheaper over 36mo
- Business tax: lease deducts full $802/mo = $9,624/yr (~$2,310/yr savings @24%); Section 179 for buy up to $28.9k yr1
- 3-yr net: LEASE $28,943 (end with nothing) vs BUY $39,053 - but buy nets +$19k equity (~$20k true net)
- Recommendation: LEASE is right choice given all 3 factors; $802/mo is a good deal
- Added "Lease-vs-Buy" tab to tracker spreadsheet (Sheet 1UL4IbDDVT5ClWb0Pqa1j_C0xgWPHtIYfUDP7q8HtBJA)
- Pre-sign: confirm money factor (<=0.0020), residual (58-60%), selling price with dealer
- Must confirm LLC name/EIN on lease + business use % with CPA

## pitrowmiami.com - REBUILT IN ASTRO (like theonegroup) - LIVE (2026-08-08)

Converted pitrowmiami from hand-rolled static HTML (Tailwind Play CDN) to a full Astro build, mirroring theonegroup-v2 architecture.

**Project:** `~/.openclaw/workspace/pitrowmiami-v2/` (new)
**Deployed to:** pitrowmiami.com (Netlify site 7abc1027), linked via `netlify link --id 7abc1027`

### Architecture (mirrors theonegroup-v2)
- Astro 7 + @tailwindcss/vite v4 (COMPILED CSS, no CDN) + @astrojs/sitemap
- `src/layouts/BaseLayout.astro` - consent-gated GA4 (G-B5NV9GJ810), meta/OG/Twitter, async fonts, cookie banner, nav/footer
- `src/styles/global.css` - F1 red theme (brand-* = #e10600 family), carbon-* dark palette, custom .card/.btn-primary/.btn-secondary/.f1-gradient/.speed-lines
- Pages: index (all sections), terms, privacy, thanks, one-sheet (print CSS preserved), 404
- `netlify/functions/book.mjs` - booking + newsletter (ADDED newsletter handling - it only did bookings before, newsletter was broken/400)

### Scores (Lighthouse)
| Category | Old static | New Astro |
|----------|-----------|-----------|
| Performance | 81 | **88** |
| Accessibility | 100 | **100** |
| SEO | 100 | **100** |
| Best-Practices | 100 | **100** |
| Terms a11y | 100 | **100** |

### Key wins
- **No Tailwind CDN** (~127KB runtime CSS) → compiled 24KB global.css. This was the big performance fix.
- **WebP images**: hero + gallery use webp (45KB vs 80KB jpg) via <picture> with jpg fallback. cwebp is at /opt/homebrew/bin/cwebp.
- **Old .html URLs redirect 301** to pretty URLs (Netlify auto handles terms.html → /terms/).
- **Newsletter form FIXED** - was posting to /api/book which only validated booking fields (always 400). Added newsletter branch to book.mjs.

### Notes / gotchas
- `netlify deploy --prod --dir dist --build` runs remote build + deploys function. Project must be LINKED to site 7abc1027 first.
- FCP ~2.8s is server/TTFB variance; CLS=0, TBT=0ms, no render-blocking. 88 is solid.
- Footer/nav brand red must be text-brand-500 (bright #ff2a20) not brand-600 (#e10600) - brand-600 fails contrast on black.
- The old static site files remain in `~/.openclaw/workspace/pitrowmiami/` (rollback option if ever needed).

## Follow-up: 1-3 done (2026-08-08 14:08)

### #1 theonegroup Astro conversion - ALREADY DONE
theonegroup.info is ALREADY on the Astro build (compiled CSS BaseLayout.2lX5QRyU.css, no CDN, consent-gated). It was the reference I mirrored for pitrowmiami. Live scores: **Perf 99, A11y 100, SEO 100, BP 100**. Nothing to convert.

### #2 Search Console sitemap verification - PARTIAL
- Both sitemaps valid + reachable (all URLs 200, referenced in robots.txt)
- NO Search Console API access: no service account, no OAuth token with webmasters scope (token.json is calendar.events only), Gmail connector broken
- To fully verify submission needs Alec: browser OAuth (webmasters scope) OR add a service account to the GSC property
- Google public index check blocked (automated query detection)

### #3 Real booking flow test - DONE ✅
- POST /api/book with full booking payload → {"success":true}
- Function only returns success after Resend response.ok, so email accepted by Resend → akennedy@theonegroup.info
- Resend key is send-only (restricted_api_key) so can't list sent emails via API
- Newsletter flow also verified working

### ⚠️ DISCOVERED: open-connector is BROKEN (pre-existing)
- `~/.openclaw/workspace/spikes/open-connector/` is EMPTY (only logs/ dir)
- launchd plist runs `npm run dev` but there's NO package.json in that dir → "Missing script: dev" error loop
- The connector source code is GONE (searched workspace, no gmail.fetch handler anywhere)
- This is why email triage has been failing. Needs rebuild or restore from backup.
- launchd.err: ~/.openclaw/workspace/spikes/open-connector/logs/launchd.err

## WhatsApp Security + Connector Rebuild (2026-08-08 20:00)

### ✅ WhatsApp security fix - DONE
- Changed `channels.whatsapp.groupPolicy` from "open" to "allowlist"
- Changed `groupAllowFrom` from ["*"] to ["+15024037201"] (Alec only)
- Security audit: 3 CRITICAL → 0 CRITICAL (groups: open=0, allowlist=1)
- Gateway restarted; log confirms: "[whatsapp] Listening for WhatsApp inbound messages (DM + all groups; sender allowlist configured)"
- Backup: openclaw.json.bak.whatsapp-sec-20260808-195926

### ✅ WhatsApp re-link - DONE (via gateway restart)
- Gateway restart re-established WhatsApp listener (was "No active WhatsApp Web listener" errors)
- Log: "[whatsapp] [default] starting provider (+15024037201)"

### ✅ open-connector REBUILT - DONE
- Source was GONE (spikes/open-connector only had logs; launchd errored "Missing script: dev")
- It's the public repo `oomol-lab/open-connector` (MCP connector: Gmail/Sheets/Calendar/HubSpot on :3000)
- Re-cloned to spikes/open-connector, npm install (525 pkgs), launchd auto-started it (RunAtLoad+KeepAlive)
- API UP on :3000 (gmail actions available), console :5173, MCP registered in openclaw.json
- Old broken dir moved to spikes/open-connector.broken-<ts>
- ⚠️ NEEDS ALEC: Google OAuth re-authorization (data dir was wiped with old clone). Console: http://localhost:5173 or http://localhost:3000/connect/gmail. Gmail returns "Connect gmail with OAuth first" until re-linked.
- OAuth app "open-connector" in GCP project clawd-agent-integrations (601004929144) still valid; credentials.json/google-credentials.json in workspace root.

### Remaining
- [ ] Alec: re-authorize Google OAuth in connector (Gmail/Sheets/Calendar)
- [ ] Alec: Search Console login to submit sitemaps (theonegroup + pitrowmiami)

## Browser automation attempt for Google OAuth (2026-08-08 20:30)
- Tried browser-use CLI (0.8.0) with OpenRouter gpt-4o-mini (vision) + Profile 1 (Alec's theonegroup account)
- CONFIRMED: browser-use profile copy does NOT carry Google session cookies (Chrome encrypts cookies with Local State key; temp copy shows "Sign in - Google Accounts" with no account)
- Autonomous agent ran 6+ min on connect flow without completing; gmail still not configured
- CONCLUSION: Google OAuth CANNOT be automated without Alec's password/2FA. Must be done manually in his real Chrome.
- Path for Alec: open http://localhost:5173 → Gmail → Connect → pick akennedy@theonegroup.info → Allow

## Connector connections restored (2026-08-08 20:40-20:50)
- Rebuilt open-connector (oomol-lab/open-connector) earlier; now re-authorized all connections
- Configured Google OAuth client (client 601004929144-7ohnnrbips3i5i83g1nh901t34n05iai) for ALL 12 Google services: gmail, googlecalendar, googlesheets, googledrive, googledocs, googleforms, googlephotos, googleslides, googletasks, google_search_console, google_analytics, google_bigquery
- Alec connected all Google services in console (localhost:5173)
- HubSpot: configured OAuth client (client c5d91ffe-3779-4ea5-9a42-a5777b70ed3a, secret bdbd05ba-812e-4b99-8e64-55c5111bad14), Alec connected. VERIFIED: search_contacts returns data, create_company works (created test company 338742571757 — NEEDS DELETION via HubSpot UI, no delete action in connector)
- GitHub: configured api_key connection with fine-grained PAT (account akennedytog, id 264555092). VERIFIED: get_current_user works
- ⚠️ Search Console API NOT enabled in GCP project 601004929144 — Alec must enable at https://console.developers.google.com/apis/api/searchconsole.googleapis.com/overview?project=601004929144 then retry submit_sitemap
- Search Console actions: list_sites, add_site, submit_sitemap, list_sitemaps, query_search_analytics, inspect_url

## Notion integration (2026-08-08 21:00)
- Notion connected via connector (api_key, account "Alec Kennedy's Space" 3b7daa91). VERIFIED: notion.search works.
- Key also stored at ~/.config/notion/api_key for ntn CLI (ntn 0.21.9 installed, needs NOTION_WORKSPACE_ID / ntn login)
- Proposed "The One Group OS" Notion structure (Alec approved): Leads, Outreach, Clients, Pipeline, Content, Competitor Intel, Testimonials databases + dashboard
- BLOCKED: Notion API can't create top-level pages (only under existing page/db). Need Alec to create ONE anchor page "The One Group OS" in Notion UI, share with integration "Alec Kennedy's Space", paste URL. Then I build all 7 databases under it.
- Browser automation can't do it (Notion session cookies don't carry to browser-use profile copy — same wall as Google/HubSpot)
- DEFERRED by Alec (21:02) — "too hard for now". Notion connected & ready; build whenever.

## Items 1, 3, 4 done (2026-08-08 21:10)

### 1. Search Console sitemaps
- Enabled Search Console API in GCP (Alec did)
- theonegroup.info: verified (siteOwner), sitemap.xml re-submitted ✅
- pitrowmiami.com: ADDED as property but siteUnverifiedUser — needs verification (DNS/HTML) before sitemap submit works. Sitemap is at sitemap-index.xml (not sitemap.xml). Connector has no verify action.
- ⚠️ pitrowmiami verification still pending (Alec: verify in Search Console UI or via DNS)

### 3. Model fallback hardening + codex pin
- Replaced gpt-4o-mini (weak) with kimi-k3 in agents.list.main.model.fallbacks; added gpt-5.6-sol as 3rd fallback. Chain now: gpt-oss:20b-cloud → kimi-k3 → gpt-5.6-sol
- Pinned codex plugin to @2026.5.28 (openclaw plugins install "@openclaw/codex@2026.5.28")
- Audit: 0 critical, 1 warn (gpt-oss:20b free fallback — acceptable), 2 info
- Backup: openclaw.json.bak.model-fix-20260808-*

### 4. HubSpot test company cleanup
- Test company "OpenClaw Connector Test 2026-08-08" (id 338742571757, Louisville) still exists
- Connector has NO delete action (manage_crm_objects is create/update only; token encrypted in connect.sqlite)
- NEEDS Alec: delete in HubSpot UI at https://app.hubspot.com/contacts/246928220/record/0-2/338742571757

## Search Console COMPLETE (2026-08-08 21:24)
- pitrowmiami.com VERIFIED via HTML file method (google5aee80ea49451334.html deployed to Netlify prod)
- Sitemap submitted: https://pitrowmiami.com/sitemap-index.xml (isPending, 0 errors)
- BOTH properties now siteOwner: theonegroup.info + pitrowmiami.com
- All Search Console work DONE ✅

## Pipeline + cron fixes (2026-08-08 21:45) — REVENUE ENGINE RESTORED

### Root cause: arlo-iris-pipeline timed out (6 consecutive errors)
- Monolithic script ran Arlo (300s) + Iris (600s) = 900s = exactly the cron timeout. Any slowness blew it.
- Arlo alone took 218s; Iris 75s. Combined exceeded 900s.

### Fixes applied:
1. SPLIT into 2 cron jobs:
   - arlo-lead-research (d7e5cebf) 8:30am ET, 1200s timeout, runs run_arlo.py
   - iris-outreach (d0cbc070) 8:45am ET, 900s timeout, runs run_iris.py
   - Removed old monolithic arlo-iris-pipeline job
2. Reduced Arlo QUERIES_PER_RUN 6→3, EMAIL_ENRICH_QUERIES 4→2 (fits timeout, still finds 20 leads)
3. FIXED Iris email-draft BUG: filter required status=="enriched" but Arlo sets "new" → 0 drafts. Changed to ("new","enriched"). Now drafts 5 emails.
4. FIXED Iris calendar BUG: dateTime lacked RFC3339 offset (naive datetime) → HTTP 400. Added ET timezone (timezone(timedelta(hours=-4))). Calendar events now create.
5. ai-news-ticker: added --max-time 30 to Tavily curl (was hanging 17min), delivery→none
6. REM Memory Promotion: cron env had Node v24.11.0 (below required 24.15). Recreated as agentTurn with PATH=/opt/homebrew/bin. 
7. twitter-ai-news-queue: delivery→none (was erroring on missing WhatsApp target; script itself worked)

### Verified working:
- Arlo: 20 new leads (11 email, 9 phone) in 218s
- Iris: 5 email drafts + 5 HubSpot companies + 5 contacts + call list in 75s
- iris-outreach cron: ok, 68.7s, delivered
- twitter-ai-news-queue cron: ok, 67ms
- arlo-lead-research cron: ok

### New files: agents/run_arlo.py, agents/run_iris.py (standalone runners)

## System Proof Report + news jobs (2026-08-08 21:55)

### System Proof Report (the "capture the receipts" play)
- Created agents/system_proof_report.py — generates data/system_proof_report.md showing what the AI system did (leads, drafts, HubSpot records)
- Weekly cron: weekly-system-proof-report (b88c328a) Mon 9am ET → WhatsApp
- Current numbers: 80 leads (28 email, 49 phone), 13 drafted, 49 call-listed, 94 HubSpot companies, 40 new leads this week, 5 drafts this week
- HubSpot count via search_companies limit=100 (no total field returned)

### Ran on demand (both OK):
- ai-news-ticker: ok, 8.2s (was 17min timeout before curl fix). Wrote 5 articles, deployed to Netlify "Deploy is live!"
- twitter-ai-news-queue: ok, 54ms, queued 2 tweets

## System Proof Report — DAILY (2026-08-08 22:00)
- Changed weekly-system-proof-report → daily-system-proof-report (b88c328a), cron 0 9 * * * (9am ET daily) → WhatsApp
- Report generator now auto-copies to Google Drive: OpenClaw-Deliverables/System-Proof-Report.md
- Verified: script runs + copies to Drive ✅

## System-proof tweet queued (2026-08-08 22:01)
- Drafted + queued "We don't just sell AI automation. We run on it." tweet to state.json twitterQueue (source: system-proof)
- Queue now has 3 tweets (2 ai-news + 1 system-proof)
- Will post at next twitter-morning/evening-post slot
- Report also copied to Google Drive + daily cron (9am ET) auto-syncs

## ⚠️ CRITICAL: theonegroup.info deploy site ID (2026-08-09)
- theonegroup.info is served by Netlify site **theonegroup-v2 (id 56446bc7-9070-44b6-84f3-aa0dd8b5277b)**
- The old memory note said site id a70736d7 (theonegroup) — WRONG for the custom domain
- Deploy method: copy dist/* to clean temp dir, `netlify link --id 56446bc7-9070-44b6-84f3-aa0dd8b5277b`, then `netlify deploy --prod --dir .`
- Do NOT deploy only a few files to temp dir — it replaces the whole site (wiped pages briefly)
- New page live: https://theonegroup.info/system-proof.html (system-proof case study)
- Accidental site magenta-douhua-522f88 (83c2672f) created during confusion — cleanup pending (delete denied)
